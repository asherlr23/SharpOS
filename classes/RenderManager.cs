using System;

class renderManager 
{
    public void RenderObject(params int[] vertexes, int writeType)
    {
        for(int i = 0; i > vertexes.Length * 2; i++) 
        {
            for 
           if(writeType == 1)
           {

           }
        }
    }
}